import type { NextPage } from "next";
import { Layout } from "src/components/Layout";
const Home: NextPage = () => {
	return (
		<Layout title="Home">
			<div className=""></div>;
		</Layout>
	);
};

export default Home;

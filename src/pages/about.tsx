import type { NextPage } from "next";
import { Layout } from "src/components/Layout";
const Home: NextPage = () => {
	return (
		<Layout title="About">
			<div className=""></div>;
		</Layout>
	);
};

export default Home;
